# OpenSpec Configuration — bunisess madngement app
# Auto-generated by sdd-init on 2026-07-04
# Hybrid persistence: openspec + Engram

project:
  name: bunisess madngement app
  path: D:\Repositories\Github Proyects\BUNISESS MADNGEMENT APP
  description: "Collaborative whiteboard application for students (similar to Excalidraw)"

context: >
  A collaborative whiteboard app targeting students. One student hosts a session,
  others join via web. Features include drawing tools, paste functionality, and
  localStorage for content persistence. Built with Next.js (React framework +
  API routes), PostgreSQL (database), Prisma ORM, and real-time collaboration
  via WebSockets (to be specified).

stack:
  framework: Next.js 14+ (React)
  database: PostgreSQL
  orm: Prisma
  realtime: WebSockets (to be determined — Socket.IO, ws, or native WebSocket)
  persistence: localStorage (client-side) + PostgreSQL (server-side)
  language: TypeScript (presumed — Next.js default)

strict_tdd: false

testing:
  runner: null
  comment: >
    No test runner detected — brand new project. When initialized, Next.js
    scaffolds with Jest/Vitest and Testing Library. Update this section once
    tooling is installed.
  layers:
    unit: null
    integration: null
    e2e: null
  coverage: null
  linter: null
  type_checker: null
  formatter: null

phases:
  proposal:
    intent_required: true
    approval: orchestrator
  spec:
    scenarios_required: true
  design:
    diagram_required: optional
  tasks:
    granularity: small
  apply:
    tdd: false
  verify:
    proof_required: true
  archive:
    delta_sync: true
